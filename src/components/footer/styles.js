import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    color: "#fff",
    marginTop: "-25px",
    maxWidth: "100%",
    zIndex: "7",
    justifyContent: "center",
    alignItems: "center",
    display: "flex",
  }, 
}));
