import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    color: "#fff",
    marginTop: "100px",
    maxWidth: "100%",
    zIndex: "1",
    justifyContent: "center",
    alignItems: "center",
    display: "flex",
  },
}));
